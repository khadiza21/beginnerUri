#include <iostream>
using namespace std;

int main()
{

    int A, B, C, D, difference;
    cin >> A >> B >> C >> D;
    difference = (A * B - C * D);
    cout << "DIFERENCA = " << difference << endl;

    return 0;
}
